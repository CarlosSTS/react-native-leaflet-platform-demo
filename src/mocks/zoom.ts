export const ZOOM_CONTROL_STYLE = `
  margin-top: 40px;
  margin-left: 20px;
  background: #1e293b;
  border: 1px solid #334155;
  border-radius: 12px;
  box-shadow: 0 6px 18px rgba(0,0,0,0.4);
        `;

export const ZOOM_CONTROL_IN_STYLE = `
  background: transparent;
  color: #ffffff;
  border-bottom: 1px solid #334155;
  font-size: 20px;
  padding: 4px 0;
        `;

export const ZOOM_CONTROL_OUT_STYLE = `
  background: transparent;
  color: #ffffff;
  font-size: 20px;
  padding: 4px 0;
        `;

export const DEFAULT_ZOOM = 11;
